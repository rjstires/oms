	  <div class="container bootcards-container" id="main">
	  	<div id="contactCard">
		  	<div class="panel panel-default">
	  			<div class="panel-heading clearfix">
	  				<h3 class="panel-title pull-left">Contact Details</h3>
	  				<div class="btn-group pull-right visible-xs">
	  					<a class="btn btn-primary" href="/contacts/eNK2QXWJa8E3/edit"
	  					data-toggle="modal"
	  					data-target="#editModal">
	  					<i class="fa fa-pencil"></i>
	  					<span>Edit</span>
	  				</a>
	  			</div>	
	  			<%= link_to(edit_admin_customer_path(@customer), class: 'btn btn-primary pull-right hidden-xs') do %>
	  			
	  			<i class="fa fa-pencil"></i>
	  			<span>Edit</span>
	  		
	  		<% end %>
	  	</div>
	  	<div class="list-group">
				<%= mail_to(@customer.email, class: 'list-group-item') do %>
					<label>Email</label>
					<h4 class="list-group-item-heading"><%= @customer.email %></h4>
				<% end %>

				<div class="list-group-item">
					<label>Battle Tag</label>
					<h4 class="list-group-item-heading"><%= @customer.battle_tag %></h4>
				</div>

				<%= render :partial => "skype_list_item", locals: {customer: @customer} unless @customer.skype.nil? %>

	  	</div>

	  </div>
	</div>
</div>
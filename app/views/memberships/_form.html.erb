<%= simple_form_for(@membership) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.association :user %>
    <%= f.association :team %>
    <%= f.input :confirmed %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
